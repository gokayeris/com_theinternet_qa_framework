[pytest]
pythonpath = .
testpaths = tests
# Esto habilita la grabación de trazas, videos y capturas automáticamente
addopts = --tracing=retain-on-failure --video=retain-on-failure --screenshot=only-on-failure